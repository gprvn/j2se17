package chap01.pack02;
/**
 *  Creating a jar File
 *  java -cvf newFile.jar . (creates a jar containing the files in current directory)
 *  
 *  java --create --verbose --file newFile.jar .
 * 
 *  Alternatively we can specify a directory instead of current directory
 *  java -cvf newFile.jar -C dir .
 * 
 *  -c, --create    creates a new JAR file
 *  -v, --verbose   prints details when working with JAR files
 *  -f <fileName>, -file <Filename>   JAR filename
 * 
 *  -C <directory> Directory containing files to be used to create the JAR
 * 
 */
public class H {
    
}
